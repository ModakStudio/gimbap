#ifndef CREATE_PROJECT_H
#define CREATE_PROJECT_H

#include "config.h"

// create_project: call the appropriate project setup function based on the selected language in the configuration.
void create_project(GimbapConfig *config);

#endif